# coding: utf8
# intente algo como
def index(): return dict(message="hello from person.py")

    response.menu += (T('Report'), False, URL('default', 'report'), [])

def persons():
    formulario =SQLFORM.grid(db.person)
    return locals()
